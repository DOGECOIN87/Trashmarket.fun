# Implementation Plan

- [x] 1. Fix WalletContext localStorage security
  - Replace direct localStorage calls with localStorageIntegrity wrapper
  - Update connect() to use setItem() for storing last wallet
  - Update disconnect() to use removeItem() for clearing last wallet
  - Update useEffect auto-reconnect to use getItem() for reading last wallet
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Fix useVanityPayment double-click vulnerability
  - [x] 2.1 Add txInFlightRef at component level
    - Create useRef<boolean>(false) for tracking in-flight transactions
    - _Requirements: 2.1, 2.3_
  - [x] 2.2 Add guard to deposit() function
    - Check txInFlightRef.current at start of function
    - Set txInFlightRef.current = true before transaction
    - Reset txInFlightRef.current = false in finally block
    - _Requirements: 2.1, 2.4_
  - [x] 2.3 Add guard to withdraw() function
    - Check txInFlightRef.current at start of function
    - Set txInFlightRef.current = true before transaction
    - Reset txInFlightRef.current = false in finally block
    - _Requirements: 2.2, 2.4_

- [x] 3. Fix dexService transaction validation and error handling
  - [x] 3.1 Add input validation to executeSwap()
    - Validate inputAmount > 0
    - Validate slippageBps between 0 and 10000
    - Validate inputMint !== outputMint
    - Validate mint address format using isValidMintAddress()
    - Return early with error object if validation fails
    - _Requirements: 3.1, 3.2, 3.5_
  - [x] 3.2 Fix skipPreflight and confirmTransaction API
    - Change skipPreflight from true to false in sendRawTransaction
    - Update confirmTransaction to use { signature, blockhash, lastValidBlockHeight } format
    - _Requirements: 3.3, 3.4_
  - [x] 3.3 Replace error.message with parseTransactionError
    - Import parseTransactionError from utils/errorMessages
    - Replace all error.message with parseTransactionError(error) in catch blocks
    - _Requirements: 3.4_

- [x] 4. Fix gorbagioMarketplace ATA creation and confirmation API
  - [x] 4.1 Add ATA creation to buildBuyTransaction()
    - Calculate ATA rent cost (2039280 lamports)
    - Update balance check to include ATA rent
    - Add try-catch block to check if buyer ATA exists
    - Add createAssociatedTokenAccountInstruction if ATA doesn't exist
    - _Requirements: 4.1, 4.2, 4.3_
  - [x] 4.2 Update executeBuy() confirmation API
    - Get blockhash and lastValidBlockHeight before sending transaction
    - Update confirmTransaction to use { signature, blockhash, lastValidBlockHeight } format
    - _Requirements: 4.4_
  - [x] 4.3 Replace error.message with parseTransactionError
    - Import parseTransactionError from utils/errorMessages
    - Replace err.message with parseTransactionError(err) in catch block
    - _Requirements: 4.4_

- [x] 5. Fix LotteryTickets error handling and confirmation API
  - [x] 5.1 Enforce feature flag in handleConvert()
    - Add early return if !IS_FEATURE_ENABLED
    - Set error status with clear message
    - _Requirements: 5.1, 5.2_
  - [x] 5.2 Update confirmTransaction API
    - Get blockhash and lastValidBlockHeight before sending transaction
    - Update confirmTransaction to use { signature, blockhash, lastValidBlockHeight } format
    - _Requirements: 5.4_
  - [x] 5.3 Replace error.message with parseTransactionError
    - Import parseTransactionError from utils/errorMessages
    - Replace err.message with parseTransactionError(err) in catch block
    - _Requirements: 5.3_

- [x] 6. Fix TrashDAQSwap double-click vulnerability
  - [x] 6.1 Add txInFlightRef at component level
    - Create useRef<boolean>(false) for tracking in-flight transactions
    - _Requirements: 6.1_
  - [x] 6.2 Add guard to handleSwap() function
    - Check txInFlightRef.current at start of function
    - Set txInFlightRef.current = true before executeSwap call
    - Reset txInFlightRef.current = false in finally block
    - _Requirements: 6.1, 6.2, 6.3_
  - [x] 6.3 Update swap button disabled condition
    - Add txInFlightRef.current to disabled condition
    - Update button text to show "Processing..." when txInFlightRef.current is true
    - _Requirements: 6.2_
  - [x] 6.4 Replace error.message with parseTransactionError
    - Import parseTransactionError from utils/errorMessages
    - Replace error.message with parseTransactionError(error) in catch block
    - _Requirements: 6.4_

- [x] 7. Fix useJunkPusherOnChain confirmation API
  - [x] 7.1 Update sendTx() confirmTransaction call
    - Ensure blockhash and lastValidBlockHeight are captured before sending
    - Update confirmTransaction to use { signature, blockhash, lastValidBlockHeight } format
    - _Requirements: 7.1, 7.2_
  - [x] 7.2 Verify skipPreflight is false
    - Check sendRawTransaction options have skipPreflight: false
    - _Requirements: 7.4_
  - [x] 7.3 Verify parseTransactionError usage
    - Confirm all catch blocks use parseTransactionError
    - _Requirements: 7.3_

- [x] 8. Fix gorbaganaRPC confirmTransaction compatibility
  - [x] 8.1 Update confirmTransaction method signature
    - Change parameter type to accept string | { signature, blockhash, lastValidBlockHeight }
    - Extract signature from either format
    - Maintain existing polling logic
    - _Requirements: 8.1, 8.2, 8.3_
  - [x] 8.2 Add timeout error message
    - Ensure timeout error is clear and actionable
    - _Requirements: 8.4_

- [x] 9. Fix NFTCard image error handling
  - [x] 9.1 Add onError handler to img element
    - Implement fallback to placeholder image
    - Prevent infinite error loops by checking current src
    - Log error for debugging
    - _Requirements: 9.1, 9.2, 9.3_
  - [x] 9.2 Create placeholder image asset
    - Add /assets/nft-placeholder.png if it doesn't exist
    - Use simple SVG or PNG placeholder
    - _Requirements: 9.2_

- [x] 10. Audit Raffle.tsx error handling
  - [x] 10.1 Verify parseTransactionError usage
    - Check all catch blocks in Raffle.tsx use parseTransactionError
    - Verify error messages are displayed in inline banners
    - _Requirements: 10.1, 10.4_
  - [x] 10.2 Verify confirmTransaction API
    - Check all confirmTransaction calls use modern API format
    - _Requirements: 10.1_
  - [x] 10.3 Verify ATA creation in createRaffle
    - Confirm creator NFT ATA existence check is present
    - Verify ATA creation instruction is added if needed
    - _Requirements: 10.3_
  - [x] 10.4 Verify input validation
    - Check all raffle parameters are validated before submission
    - Verify clear error messages for validation failures
    - _Requirements: 10.2_
